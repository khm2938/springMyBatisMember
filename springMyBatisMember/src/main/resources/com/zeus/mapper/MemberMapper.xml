<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zeus.mapper.MemberMapper">

    <resultMap type="Member" id="memberMap">
        <id property="no" column="no" />
        <result property="id" column="id" />
        <result property="pw" column="pw" />
        <result property="name" column="name" />
        <result property="regDate" column="regdate" />
        <result property="modDate" column="moddate" />
        <collection property="authList" resultMap="authMap" />
    </resultMap>

    <resultMap type="MemberAuth" id="authMap">
        <result property="no" column="no" /> 
        <result property="auth" column="auth" />
    </resultMap>

    <select id="read" resultMap="memberMap">
        SELECT m.no, 
               m.id, 
               m.pw, 
               m.name, 
               m.regdate AS "regDate"
               a.auth
        FROM MYBATISMEMBER m 
        LEFT OUTER JOIN MYBATISMEMBERAUTH a ON m.no = a.no
        WHERE m.no = #{no} and m.enabled = '1'
    </select>

    <select id="list" resultMap="memberMap">
        SELECT no, id, pw, name
        FROM MYBATISMEMBER 
        WHERE enabled = '1'
        ORDER BY regDate DESC
    </select>
    
    <select id="search" resultType="Member">
        SELECT * FROM MYBATISMEMBER 
        <where>
            <if test="keyword != null and keyword != ''">
                <choose>
                    <when test="searchType == 'id'">
                        AND ID LIKE '%' || #{keyword} || '%'
                    </when>
                    <when test="searchType == 'name'">
                        AND NAME LIKE '%' || #{keyword} || '%'
                    </when>
                    <otherwise>
                        AND NAME LIKE '%' || #{keyword} || '%'
                    </otherwise>
                </choose>
            </if>
        </where>
        AND ENABLED = '1' ORDER BY no DESC
    </select>

    <update id="update">
        UPDATE MYBATISMEMBER
        SET name = #{name}, 
        pw = #{pw} 
        WHERE no = #{no} and enabled = '1'
    </update>

	<!-- Soft Delete -->
    <delete id="delete">
    	UPDATE MYBATISMEMBER 
    	SET enabled = '0' 
        WHERE no = #{no}
		<!-- Hard Delete => DELETE FROM MYBATISMEMBER WHERE no = #{no} -->        
        
    </delete>

    <delete id="deleteAuth">
        DELETE FROM MYBATISMEMBERAUTH
        WHERE no = #{no}
    </delete>

    <insert id="create" parameterType="Member">
        <selectKey keyProperty="no" resultType="int" order="BEFORE">
            SELECT mybatismember_seq.NEXTVAL FROM DUAL
        </selectKey>
        INSERT INTO MYBATISMEMBER (no, id, pw, name)
        VALUES (#{no}, #{id}, #{pw}, #{name})
    </insert>

    <insert id="createAuth">
        INSERT INTO MYBATISMEMBERAUTH (no, auth)
        VALUES (#{no}, #{auth})
    </insert>

</mapper>